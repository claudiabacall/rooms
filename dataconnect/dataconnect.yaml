specVersion: "v1"
serviceId: "roomscode"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "roomscode-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
